<?php
     

        $host="localhost";
        $db="maturski";
        $username="root";
        $pass="";

        $connection = mysqli_connect(
            hostname:$host,
            username:$username,
            password:$pass,
            database:$db
            );
   ?>